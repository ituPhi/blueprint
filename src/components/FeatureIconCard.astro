---
import { Icon } from "astro-icon/components";
// Importing the Image component from Astro's component library
import Image from "astro/components/Image.astro";
// Destructuring title, description, and iconSrc from the component's props
const { title, description, iconSrc } = Astro.props;
---

<div class="flex flex-col gap-4 text-center items-center">
  <!-- Icon container with a predefined style -->
  <!-- <div class="flex justify-center bg-orion-blue-50 rounded-full w-16 h-16"> -->
  <!-- Static icon image (currently hardcoded to favicon.svg) -->

  <!-- <Icon name={"service"} width={50} height={50} class="text-orion-gray-500" /> -->
  <!-- </div> -->
  <!-- Title of the feature card, styled and dynamically set -->
  <h3 class="text-2xl font-semibold text-orion-gray-600 pt-4">{title}</h3>
  <!-- Description of the feature card, styled and dynamically set -->
  <p class="text-lg text-orion-gray-600">{description}</p>
</div>
